{{- if .Values.reverseProxy.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  name: {{ .Values.reverseProxy.name }}-ingress
  labels:
    app: {{ .Values.reverseProxy.name }}
spec:
  {{- if .Values.reverseProxy.ingress.className }}
  ingressClassName: {{ .Values.reverseProxy.ingress.className }}
  {{- end }}
  rules:
    - host: {{ .Values.reverseProxy.ingress.host }}
      http:
        paths:
          - path: {{ .Values.reverseProxy.ingress.path }}
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.reverseProxy.name }}
                port:
                  number: {{ .Values.reverseProxy.service.port }}
  {{- if .Values.reverseProxy.ingress.tls }}
  tls:
    - hosts:
        - {{ .Values.reverseProxy.ingress.host }}
      secretName: {{ .Values.reverseProxy.ingress.tlsSecretName }}
  {{- end }}
  {{- end }}
